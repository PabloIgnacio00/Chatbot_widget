import React from 'react';


const Chatbot = ({ token }) => {
  // You can use the token value within your component to customize its behavior
  // For example, you can make API calls or display different content based on the token

  // Example usage: Displaying the token value
  return (
    <div>
      <h1>My Component</h1>
      <p>Token: {token}</p>
      <p>This is an example React component.</p>
    </div>
  );
};

export default MyComponent;
